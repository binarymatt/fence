- model: Policy
  rows:
    - id: policy0
      content: permit(principal == User::"alice",action == Action::"view",resource in Album::"jane_vacation");
      created_at: "{{ now }}"
      updated_at: "{{ now }}"

- model: Entity
  rows:
    - id: VacationPhoto94.jpg
      type: Photo
      parents:
        - id: jane_vacation
          type: Album
      created_at: "{{ now }}"
      updated_at: "{{ now }}"
    - id: alice
      type: User
      created_at: "{{ now }}"
      updated_at: "{{ now }}"
    - id: bob
      type: User
      parents:
        - id: people
          type: Group
      created_at: "{{ now }}"
      updated_at: "{{ now }}"
